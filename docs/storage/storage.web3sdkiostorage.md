---
slug: /storage.web3sdkiostorage
title: Web3sdkioStorage class
hide_title: true
displayed_sidebar: storage
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

## Web3sdkioStorage class

Upload and download files from decentralized storage systems.

**Signature:**

```typescript
export declare class Web3sdkioStorage<T extends UploadOptions = IpfsUploadBatchOptions>
```

## Example

```jsx
// Create a default storage class without any configuration
const storage = new Web3sdkioStorage();

// Upload any file or JSON object
const uri = await storage.upload(data);
const result = await storage.download(uri);

// Or configure a custom uploader, downloader, and gateway URLs
const gatewayUrls = {
  // We define a mapping of schemes to gateway URLs
  "ipfs://": [
    "https://gateway.ipfscdn.io/ipfs/",
    "https://cloudflare-ipfs.com/ipfs/",
    "https://ipfs.io/ipfs/",
  ],
};
const downloader = new StorageDownloader();
const uploader = new IpfsUploader();
const storage = new Web3sdkioStorage({ uploader, downloader, gatewayUrls });
```

## Constructors

| Constructor                                                          | Modifiers | Description                                                         |
| -------------------------------------------------------------------- | --------- | ------------------------------------------------------------------- |
| [(constructor)(options)](./storage.web3sdkiostorage._constructor_.md) |           | Constructs a new instance of the <code>Web3sdkioStorage</code> class |

## Properties

| Property                                                | Modifiers | Type                                    | Description |
| ------------------------------------------------------- | --------- | --------------------------------------- | ----------- |
| [gatewayUrls](./storage.web3sdkiostorage.gatewayurls.md) |           | [GatewayUrls](./storage.gatewayurls.md) |             |

## Methods

| Method                                                                 | Modifiers | Description                                                                                                                                                                        |
| ---------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [download(url)](./storage.web3sdkiostorage.download.md)                 |           | Downloads arbitrary data from any URL scheme.                                                                                                                                      |
| [downloadJSON(url)](./storage.web3sdkiostorage.downloadjson.md)         |           | Downloads JSON data from any URL scheme. Resolves any URLs with schemes to retrievable gateway URLs.                                                                               |
| [resolveScheme(url)](./storage.web3sdkiostorage.resolvescheme.md)       |           | Resolve any scheme on a URL to get a retrievable URL for the data                                                                                                                  |
| [upload(data, options)](./storage.web3sdkiostorage.upload.md)           |           | Upload arbitrary file or JSON data using the configured decentralized storage system. Automatically uploads any file data within JSON objects and replaces them with hashes.       |
| [uploadBatch(data, options)](./storage.web3sdkiostorage.uploadbatch.md) |           | Batch upload arbitrary file or JSON data using the configured decentralized storage system. Automatically uploads any file data within JSON objects and replaces them with hashes. |
